<%= form_for(@payment_user) do |f| %>
  <% if @payment_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@payment_user.errors.count, "error") %> prohibited this payment_user from being saved:</h2>

      <ul>
      <% @payment_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
